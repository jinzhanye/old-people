<!--components/pager/index.wxml-->
<view
  class="container">
  <image
    class="navi-icon"
    src="{{ leftImg }}"
    bind:tap="onTapLeft"/>
  <text>{{ title }}</text>
  <image
    class="navi-icon"
    src="{{ rightImg }}"
    bind:tap="onTapRight"/>
</view>
